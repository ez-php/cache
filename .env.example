# APP
APP_NAME=ez-php-cache
APP_ENV=local
APP_DEBUG=true

# Redis
REDIS_HOST=redis
REDIS_PORT=6379        # connection port used inside the container (always 6379)
REDIS_HOST_PORT=6380   # host-side port mapping in docker-compose; distinct from root project's 6379
